# react-fastapp-sample
this is a sample application


## migration to db

### init command
```bash
pip install alembic
alembic init alembic
```


### access mysql
```bash
docker exec -it mysql /bin/bash
mysql -u root -p

```